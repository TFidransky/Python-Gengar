Cílem programu je získat CSV s několika sloupci, ve kterých je zaznamenán datum (denní interval) a přepočítat to na týdenní a roční průměr.

def openCSV():
	Prvně je tedy potřeba otevřít žádaný soubor ve formátu "vstup.csv" (je možno změnit na 7. řádku pro jiný název souboru - nutno zachovat formát souboru!)
	Dále proběhne try blok, který řeší možné výjimky (chyby) a v případě chyby vyhodí příslušný error do konzole. ; pozn. - původně jsem celý program chtěl dělat bez pandas, ale nějak jsem nepřišel na to jak elegantně ošetřit prázdný soubor a všude všichni používají pandas.

def parseToWeeks (rows: []):
	celé týdny:
		rozdělí jednotlivé dny na 7denní sekvence, ty potom uloží jako weeks a do nich přiřadí hodnoty dnů (day)
		počítá to s proměnnou i, dosáhne potom 7, což je důležité pro nekompletní týdny

	nekompletní týdny:
		pozná to, pokud i /= 7 (tedy není to celý týden), poté to převede na týden

def calculateWeekAverage(week):
	proměnná amounts, do ní se pak vkládá výpočet průměru (suma / počet) za dny v daném týdnu

def writeWeekToCsv(first_day, average):
	vytvoří nový soubor ("vystup_7dni.csv), tentokrát v psacím formátu ("w")
	vezme si to první den daného týdne a k němu to vezme průměrnou hodnotu průtoku za celý týden (zaokr. na 4 des. místa)

def calculateYearAverage(year):
	

def WriteYearToCSV(first_day, average):
	stejný postup jak u writeWeekToCsv, ale interval je roční a zapisuje do "vystup_rok.csv"

def min_max (rows):
	výpočet a následné vypsání minima a maxima do konzole - vypíše datum a hodnotu průtoku.
	max - hodnota průtoku
	max_day - příslušný den, kdy byl nejvyšší průtok
	to samé pro min a min_day
	poté projede for interval (pro min a max zvlášť) a postupně projíždí jednotlivé řádky a porovnává hodnoty.

def main():





